[pytest]
# Put the project root (for card_detector, centering, ...) and the tests dir
# (for the conftest synthetic-card helpers) on the import path.
pythonpath = . tests
testpaths = tests
addopts = -q
