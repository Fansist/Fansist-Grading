# Trading Card Grader -- minimal, no ML frameworks.
# Pinned to lower bounds known to provide the APIs used here.
streamlit>=1.32
opencv-python-headless>=4.8
numpy>=1.24
# QR code on the slab + the web report page it links to:
qrcode[pil]>=7.4
flask>=3.0
